# Local .terraform directories
**/.terraform/*

# .tfstate files
*.tfstate
*.tfstate.*

# Crash log files
crash.log

# Exclude any .tfvars files, which are likely to contain sensitive data, such as passwords, private keys, and other secrets.
*.tfvars

# Ignore override files as they are usually used to override resources locally and so are not checked in
override.tf
override.tf.json
*_override.tf
*_override.tf.json

# Include .terraformrc and terraform.rc files as they are configuration files for Terraform CLI
.terraformrc
terraform.rc

# Ignore plan output files
*.tfplan

# Ignore .DS_Store file (macOS specific)
.DS_Store